---
date: 2026-05-20
day: 星期三
week: 21
---
# 2026-05-20

## 待办事项
- [ ] 优先提交 `v96_backtrack` 验证线上分数，确认能否突破当前 `366.25` 的并列最佳
- [ ] 以 `v62 / v77 / v87` 为稳定锚点，对照 `v95 / v96` 复盘 `proxy_8/9` 与 `medium_31 + proxy_11/12` 这几簇失配信号
- [ ] 把这轮刷榜的有效家族与无效方向收成一页判断，避免继续重复试 `eq-rank`、过宽 tie-break 和无效组合
- [ ] 如果 `v96` 不能稳定超过锚点，就回到 `v87` 家族继续做小步、可解释的局部修补

## 日志
*

## 备注
- `2026-05-17` 到 `2026-05-19` 没有单独写日报；今天这篇按 `/Users/wangran/Desktop/code/submission-test/SCORES.md` 回收这几天的真实主线。
- 这几天工作几乎都在刷榜，当前主线已经非常集中：围绕华为挑战赛的 NSLB 解法做版本迭代、本地筛选和线上提交验证。
- `SCORES.md` 里能看到一条比较清楚的爬升路径：`v33 356` → `v43 / v46 359` → `v47 / v49 / v51 362.8x` → `v62 / v77 / v87 366.25`。
- 当前线上并列最佳分数是 `366.25`，对应 `v62 / v77 / v87`；`v95` 已经追到 `366.24`，`v96` 仍待提交，但本地 `submit_core` 已有 `+16.06`，因此是今天最值得优先验证的候选。
- 从最近几轮实验看，`eq-rank`、过宽的 rank tie-break 和组合增强并没有稳定对齐线上，当前更值得信任的仍是 `v62 / v87` 这一条锚点家族。
- 当前 `20_项目` 里有 5 个 `active` 项目，都已停更超过 3 天；今天不建议回补旧项目，先把刷榜这条主线继续压实。
- 当前收件箱有 2 条 `pending`；[[research agent项目]] 仍挂在收件箱，但这条线已经接近正式项目颗粒度，如果刷榜还会持续，适合尽快 `/kickoff`。今日记录入口已创建为 [[20260520记录自己]]。

## AI 摘要
这几天不是在分散推进多个方向，而是几乎把全部精力都压在刷榜上了。当前最重要的不是再开新方向，而是尽快验证 `v96_backtrack`，并把已经证明有效的 `v62 / v87` 锚点家族和已经反复失效的 `eq-rank` 路线正式分开；这样后续每一步迭代才不会继续在同样的坑里打转。

## 相关项目
- [[research agent项目]] - 当前实质主线已经变成华为挑战赛刷榜与 NSLB 版本迭代
- [[调度算法仿真器与算法实现]] - 近期未更新，今天不作为主线

---

**精力:** ⚡⚡⚡⚡⚡ | **专注:** 🎯🎯🎯🎯🎯
